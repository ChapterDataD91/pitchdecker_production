// Phase-A stub. Phase-C will replace with persona cards.
import type { PersonasSection } from '@/lib/types'
import type { Brand } from '../brand'
import { sectionPlaceholder } from './_placeholder'

export function renderPersonas(_data: PersonasSection, _brand: Brand): string {
  return sectionPlaceholder('Three Candidate Personas — rendered in Phase C')
}
