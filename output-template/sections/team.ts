// Phase-A stub. Phase-B will replace with real Lead Team + Network grids.
import type { TeamSection } from '@/lib/types'
import type { Brand } from '../brand'
import { sectionPlaceholder } from './_placeholder'

export function renderTeam(_data: TeamSection, _brand: Brand): string {
  return sectionPlaceholder('Team section — rendered in Phase B')
}
