// Phase-A stub. Phase-B will replace with salary callout.
import type { SalarySection } from '@/lib/types'
import type { Brand } from '../brand'
import { sectionPlaceholder } from './_placeholder'

export function renderSalary(_data: SalarySection, _brand: Brand): string {
  return sectionPlaceholder('Expected Salary Package — rendered in Phase B')
}
