// Phase-A stub. Phase-C will replace with process timeline.
import type { TimelineSection } from '@/lib/types'
import type { Brand } from '../brand'
import { sectionPlaceholder } from './_placeholder'

export function renderTimeline(_data: TimelineSection, _brand: Brand): string {
  return sectionPlaceholder('Process & Timeline — rendered in Phase C')
}
