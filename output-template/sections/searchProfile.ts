// Phase-A stub. Phase-B will replace with must-haves + nice-to-haves + personality.
import type { SearchProfileSection } from '@/lib/types'
import type { Brand } from '../brand'
import { sectionPlaceholder } from './_placeholder'

export function renderSearchProfile(_data: SearchProfileSection, _brand: Brand): string {
  return sectionPlaceholder('Search Profile section — rendered in Phase B')
}
